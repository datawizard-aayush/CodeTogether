import { initials } from '../data/demoData';
export default function ChatPanel({ repo, user, draft, setDraft, sendMessage }) { return <section className="chat card"><div className="chat-head"><div><h2>#{repo.name.toLowerCase().replace(/\s+/g, '-')}</h2><small>Repository team chat · local mode</small></div><b>● {repo.members.filter((member) => member.online).length} online</b></div><div className="messages">{repo.messages.map((message) => <article className={message.authorId === user.id ? 'mine' : ''} key={message.id}><span className="avatar">{initials(message.author)}</span><div className="bubble"><header><strong>{message.author}</strong><time>{message.createdAt}</time></header><p>{message.text}</p></div></article>)}</div><form className="composer" onSubmit={sendMessage}><input value={draft} onChange={(event) => setDraft(event.target.value)} placeholder="Message repository members..." /><button>Send</button></form></section>; }
