export default function Topbar({ query, setQuery, openInvite, openAi, flash }) { return <header className="topbar"><label className="search"><span>⌕</span><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search repositories, chats or members" /></label><div className="top-actions"><button type="button" className="ai-trigger-btn" onClick={openAi}>🤖 AI Assistant</button><button type="button" onClick={() => flash('No new notifications')}>Bell</button><button type="button" onClick={openInvite}>Invite</button></div></header>; }
