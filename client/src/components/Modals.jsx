function Modal({ title, close, children }) { return <div className="modal-bg" onClick={close}><div className="modal card" onClick={(event) => event.stopPropagation()}><header><h2>{title}</h2><button onClick={close}>×</button></header>{children}</div></div>; }
export function CreateRepoModal({ form, setForm, submit, close }) { return <Modal title="Create repository" close={close}><form onSubmit={submit} className="form"><label>Name<input autoFocus value={form.name} onChange={(event) => setForm({ ...form, name: event.target.value })} placeholder="mobile-app" /></label><label>Description<textarea value={form.description} onChange={(event) => setForm({ ...form, description: event.target.value })} /></label><label>Language<select value={form.language} onChange={(event) => setForm({ ...form, language: event.target.value })}><option>JavaScript</option><option>TypeScript</option><option>Node.js</option><option>Python</option></select></label><label className="check"><input type="checkbox" checked={form.isPrivate} onChange={(event) => setForm({ ...form, isPrivate: event.target.checked })} /> Private repository</label><footer><button type="button" onClick={close}>Cancel</button><button className="primary">Create repo</button></footer></form></Modal>; }
export function InviteModal({ repo, email, setEmail, submit, close }) { return <Modal title={`Invite to ${repo?.name || 'repository'}`} close={close}><form onSubmit={submit} className="form"><p>Invitations will connect to the backend in the next phase.</p><label>Member email<input autoFocus type="email" value={email} onChange={(event) => setEmail(event.target.value)} placeholder="teammate@example.com" /></label><footer><button type="button" onClick={close}>Cancel</button><button className="primary">Prepare invite</button></footer></form></Modal>; }
