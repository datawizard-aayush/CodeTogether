import { initials } from '../data/demoData';
export default function MemberPanel({ repo, openInvite }) { return <aside className="right"><section className="card panel"><header><h3>Members</h3><button onClick={openInvite}>Manage</button></header>{repo.members.map((member) => <div className="member" key={member.id}><span className="avatar alt">{initials(member.name)}</span><span><strong>{member.name}</strong><small>{member.role} · {member.online ? 'Online' : 'Away'}</small></span><i className={member.online ? 'online' : ''} /></div>)}<button className="invite" onClick={openInvite}>+ Invite member</button></section><section className="card panel"><h3>Recent activity</h3><ul><li>✓ Repository workspace created</li><li>💬 Chat is available to members</li><li>📁 MongoDB integration can be added later</li></ul></section></aside>; }
