export default function ActivityView({ repos }) { return <section className="activity"><p className="eyebrow">Workspace / activity</p><h1>Recent activity</h1><p>Everything happening across your local repositories.</p><div className="activity-grid">{repos.map((repo) => <article className="card" key={repo.id}><i style={{ background: repo.color }} /><div><strong>{repo.name}</strong><p>{repo.messages.length ? `${repo.messages.length} conversation messages.` : 'No messages yet. Start the conversation.'}</p></div><small>{repo.isPrivate ? 'Private' : 'Public'}</small></article>)}</div></section>; }
